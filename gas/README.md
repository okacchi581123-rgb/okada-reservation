# gas/

将来、[clasp](https://github.com/google/clasp) を用いて本番 Google Apps Script (GAS) プロジェクトをGitHub管理する際の格納先。

現時点ではこのディレクトリは空であり、GASコードは含まれていない。本番GASの内容を推測して生成することはしない。導入時は、実際のGASプロジェクトから `clasp pull` で取得したコードを、レビューのうえで取り込む。

詳細は [docs/AUTOMATION.md](../docs/AUTOMATION.md) を参照。
